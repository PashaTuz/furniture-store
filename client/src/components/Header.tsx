// Імпортуємо компонент Link з бібліотеки react-router-dom.
// Link замінює стандартний тег <a>, дозволяючи перемикати сторінки миттєво без перезавантаження вкладки.
import { Link } from "react-router-dom";

export default function Header() {
  return (
    // <header> — семантичний тег для шапки сайту.
    // bg-slate-800: темно-сірий колір фону.
    // p-4: внутрішні відступи (padding) з усіх боків.
    // shadow-md: додає невелику тінь знизу, щоб "підняти" шапку над контентом.
    <header className="bg-slate-800 text-white p-4 shadow-md">
      
      {/* Контейнер для центрування вмісту:
          container mx-auto: обмежує ширину і ставить по центру.
          flex justify-between: розштовхує логотип і меню по різних боках.
          items-center: вирівнює текст по вертикалі рівненько в одну лінію.
      */}
      <div className="container mx-auto flex justify-between items-center">
        
        {/* Логотип магазину.
            to="/": вказує, що при кліку ми завжди потрапимо на головну сторінку.
            text-teal-400: бірюзовий колір тексту для акценту.
        */}
        <Link to="/" className="text-2xl font-bold text-teal-400">
          🛋️ FurnitureStore
        </Link>
        
        {/* Навігаційне меню:
            space-x-6: додає однаковий відступ між усіма посиланнями всередині.
        */}
        <nav className="space-x-6">
          
          {/* Посилання на головний каталог */}
          <Link to="/" className="hover:text-teal-400 transition">
            Каталог
          </Link>

          {/* Посилання на сторінку кошика */}
          <Link to="/cart" className="hover:text-teal-400 transition">
            Кошик
          </Link>
          
          {/* Посилання на профіль користувача.
              text-slate-300: робимо колір трохи тьмянішим, ніж у головних кнопок.
          */}
          <Link to="/profile" className="hover:text-teal-400 transition text-slate-300">
            Профіль
          </Link>

          {/* Посилання на панель адміністратора.
              hover:text-red-400: при наведенні мишкою текст стане червоним.
          */}
          <Link to="/admin" className="hover:text-red-400 transition text-slate-300">
            Адмінка
          </Link>
          
        </nav>
      </div>
    </header>
  );
}