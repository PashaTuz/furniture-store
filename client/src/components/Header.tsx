// Імпортуємо компонент Link з бібліотеки react-router-dom.
import { Link } from "react-router-dom";

export default function Header() {
  return (
    // <header> — семантична шапка.
    // bg-slate-800: темно-сірий фон.
    // p-4: відступи.
    // shadow-md: тінь для об'єму.
    <header className="bg-slate-800 text-white p-4 shadow-md">
      
      {/* Контейнер для вмісту:
          container mx-auto: центрування.
          flex: вмикаємо гнучку розмітку.
          flex-col: на мобільних телефонах елементи стають один під одним (вертикально).
          md:flex-row: на екранах від 768px (планшети/ПК) повертаємо в один рядок.
          justify-between: розштовхуємо лого та меню по боках (на ПК).
          items-center: центрування по вертикалі.
          gap-4: відступ між логотипом і меню, коли вони в мобільному режимі (стовпчиком).
      */}
      <div className="container mx-auto flex flex-col md:flex-row justify-between items-center gap-4">
        
        {/* Логотип магазину.
            text-2xl: великий шрифт.
            text-teal-400: бірюзовий колір.
        */}
        <Link to="/" className="text-2xl font-bold text-teal-400">
          🛋️ FurnitureStore
        </Link>
        
        {/* Навігаційне меню:
            flex: дозволяє кнопкам гнучко розміщуватися.
            flex-wrap: якщо посилання не влізуть в один рядок, вони перенесуться нижче (важливо для маленьких екранів).
            justify-center: центруємо кнопки на телефонах.
            space-x-4: невеликий відступ між кнопками на мобілці.
            md:space-x-6: збільшений відступ між кнопками на комп'ютерах.
        */}
        <nav className="flex flex-wrap justify-center space-x-4 md:space-x-6">
          
          {/* Посилання на каталог.
              text-sm: трохи менший текст на мобільних.
              md:text-base: стандартний розмір тексту на ПК.
          */}
          <Link to="/" className="hover:text-teal-400 transition text-sm md:text-base">
            Каталог
          </Link>

          {/* Посилання на кошик */}
          <Link to="/cart" className="hover:text-teal-400 transition text-sm md:text-base">
            Кошик
          </Link>
          
          {/* Посилання на профіль */}
          <Link to="/profile" className="hover:text-teal-400 transition text-slate-300 text-sm md:text-base">
            Профіль
          </Link>

          {/* Посилання на адмінку */}
          <Link to="/admin" className="hover:text-red-400 transition text-slate-300 text-sm md:text-base">
            Адмінка
          </Link>
          
        </nav>
      </div>
    </header>
  );
}