// "export default" робить цей компонент доступним для імпорту в App.tsx.
// Кожна сторінка в React — це функція, яка повертає HTML-подібну розмітку (JSX).
export default function Product() {
  return (
    // text-center центрує весь текст всередині блоку.
    <div className="text-center">
      {/* h2 — головний заголовок сторінки. 
          text-3xl робить текст великим, font-bold — жирним. */}
      <h2 className="text-3xl font-bold">Деталі товару 🛋️</h2>
      
      {/* p (параграф) для опису. 
          mt-4 додає відступ зверху (margin-top).
          text-slate-400 робить колір тексту приглушеним сірим. */}
      <p className="mt-4 text-slate-400">
        Тут буде відображатися повна інформація про обрані меблі: ціна, характеристики та матеріали.
      </p>
    </div>
  );
}