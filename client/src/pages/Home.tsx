// export default дозволяє React Router (або App.tsx) імпортувати цю сторінку
// за її назвою (Home).
export default function Home() {
  return (
    // <h2> — заголовок другого рівня. Використовується для назви секції або сторінки.
    // text-3xl: встановлює великий розмір шрифту (приблизно 30px).
    // font-bold: робить текст жирним, щоб підкреслити, що це заголовок.
    <h2 className="text-3xl font-bold">
      🏠 Головна сторінка каталогу
    </h2>
  );
}